<?xml version="1.0" encoding="utf-8"?>

<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="#ffffffff">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="@dimen/pinlock_layout_margin_start"
        android:layout_width="@dimen/pinlock_layout_width"
        android:layout_height="match_parent">

        <in.arjsna.passcodeview.PassCodeView
            android:id="@+id/pass_code_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="@dimen/pinlock_padding"
            app:digits="4"
            app:digit_spacing="@dimen/pinlock_digit_spacing"
            app:digit_vertical_padding="@dimen/pinlock_digit_vertical_padding"
            app:digit_size="@dimen/pinlock_digit_size"
            app:key_text_size="@dimen/pinlock_key_text_size"
            app:empty_drawable="@drawable/empty_dot"
            app:filled_drawable="@drawable/filled_dot" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>
